<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PicKit" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.PicKit.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PicKit.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PicKit.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PhotoMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">?attr/colorPrimaryVariant</item>
    </style>

    <style name="PhotoMenu.TextStyle">
        <item name="android:textColor">@color/lightwhite</item>
        <item name="android:textSize">15sp</item>
        <item name="android:gravity">start</item>
    </style>
</resources>